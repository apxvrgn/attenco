import { ChatHostDirective } from './chat-host.directive';

describe('ChatHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatHostDirective();
    expect(directive).toBeTruthy();
  });
});
